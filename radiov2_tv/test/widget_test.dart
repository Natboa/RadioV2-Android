import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // TV app requires AudioService and DB — integration tested on device.
    expect(true, isTrue);
  });
}
